import type { ToolPanelProps } from '../toolTypes';

// Implemented in the shared-staff step.
export function SharedStaffPanel(_props: ToolPanelProps) {
  return <section className="tool-panel" />;
}
