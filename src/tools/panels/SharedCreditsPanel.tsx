import type { ToolPanelProps } from '../toolTypes';

// Implemented in the shared-credits step.
export function SharedCreditsPanel(_props: ToolPanelProps) {
  return <section className="tool-panel" />;
}
