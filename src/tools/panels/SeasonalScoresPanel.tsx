import type { ToolPanelProps } from '../toolTypes';

// Implemented in the seasonal-scores step.
export function SeasonalScoresPanel(_props: ToolPanelProps) {
  return <section className="tool-panel" />;
}
