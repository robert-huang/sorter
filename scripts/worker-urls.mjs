/** Deployed Cloudflare Worker URLs — keep in sync with `wrangler deploy` output. */
export const ANIPLAYLIST_PROXY_URL = 'https://sorter.sorter-aniplaylist-proxy.workers.dev';
export const MAL_PROXY_URL = 'https://sorter-mal-proxy.sorter-aniplaylist-proxy.workers.dev/mal';
